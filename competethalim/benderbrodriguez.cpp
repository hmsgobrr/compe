#include <bits/stdc++.h>
using namespace std;
int L;
char[2] rot, cur;
int main() {
    for (;cin >> L, L;) {
        cur = "+x";
        for (L--;L--;) {
            cin >> rot;
            if (rot == "No") continue;
            else if (cur == "+x") 
        }
    }
}
